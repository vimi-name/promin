# promin profile layers

Profile layers are trusted, declarative defaults used by the guided experience.
They are composed into a revisioned project-specific `ResolvedProfile`; they are
not authority and cannot grant capabilities. Repository facts and explicit user
requirements may refine or replace compatible layers.
